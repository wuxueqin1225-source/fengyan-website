# 峰妍新材料官网 · 账号与系统交付手册

> 整理时间：2026-09-01
> 适用问题：目前 GitHub / Cloudflare / Resend / 域名全在个人账号名下，如何把整套系统交给别人
> 配套文档：《部署全流程与运维手册.md》

---

## 一、先说结论：哪些能过户，哪些只能重建

这是整个交付动作的分水岭，先看清再动手：

| 资产 | 现在归属 | 能直接过户吗 | 实际做法 |
|---|---|---|---|
| 域名 `fengyanpigment.com` | 腾讯云个人账号 | ✅ 可以 | 腾讯云「域名过户」，或只改 DNS 不改所有权 |
| DNS 解析记录 | 腾讯云 DNSPod | 随域名走 | 导出快照，交接给对方 |
| GitHub 仓库 | `wuxueqin1225-source/fengyan-website` | ✅ 可以 | Settings → General → Transfer ownership |
| **Cloudflare Pages 项目** | 个人账户 `bc96fbfd…` | ❌ **不能跨账号转移** | 在对方账号**重建**一个 Pages 项目 |
| **Cloudflare 环境变量 / 密钥** | 同上 | ❌ | 新账号重新填三个变量 |
| **Resend 域名验证** | 个人 Resend 账号 | ❌ **不能转移** | 对方账号重新添加并验证域名 |
| **Resend API Key** | 同上 | ❌ | 新账号重新生成，旧的 Revoke |
| 收件邮箱 `Huangjianhui1978@163.com` | 黄建辉个人 163 邮箱 | — | 改 `NOTIFY_EMAIL` 为对方邮箱（改完需重新部署） |

**核心认知**：所谓的「交付」，本质是——**在对方账号里重建一套 → 并行验证跑通 → 把 DNS 切过去 → 下线你自己账号里的资源**。

好消息是：因为全站无构建、无数据库、无服务器（纯静态 + 一个 Functions），重建成本极低，而且新旧两套可以**同时在线并行**，切换是改一条 CNAME 的事，几乎可以做到零停机。

---

## 二、三条路径，先选一条

### 路径 A · 完全过户（账号全部迁到对方名下）

适合：离职交接、项目卖断、你不再参与维护。

代价：1–2 小时操作 + DNS/证书生效等待（最长 48 小时）。有一个硬中断点（见 3.4）。

---

### 路径 B · 只交管理权，账号仍在你名下

适合：「我帮你搭好，你自己维护，但账号先挂我这儿」。

| 平台 | 操作 | 用时 |
|---|---|---|
| GitHub | Settings → Collaborators → Add people（给 **Admin**） | 1 分钟 |
| Cloudflare | Manage Account → Members → Invite（角色选 **Cloudflare Pages: Edit**，最小权限） | 2 分钟 |
| Resend | Settings → Team → Invite teammate | 1 分钟 |
| 腾讯云 DNS | CAM 子账号授权，或直接给 DNSPod 子账号 | 5 分钟 |

- ✅ 零停机、5 分钟搞定、随时可收回
- ❌ 账单、合规责任仍在你；你仍是实际控制人，等于留了后门

> 说实话，如果是给公司做的，这条**不建议长期用**。账号挂个人名下，万一你换工作、账号被封、双因素丢了，对方直接瘫痪。

---

### 路径 C · 只交代码，对方完全自建（最干净）

适合：交给客户的公司 IT，或对方本来就有自己的云账号。

你只需要交付一份「重建包」，对方照《部署全流程与运维手册》第三章自己搭，全程不用你的账号。

- ✅ 你和这套系统彻底解耦，零责任
- ❌ 对方需要具备基本的 Git / DNS 操作能力

**我推荐这条**，除非对方明确要求你帮他搭好。

---

## 三、路径 A 详细操作步骤（按顺序来）

### 3.0 交接前必做：轮换密钥

⚠️ **这一步别跳过。** Resend API Key 和 Cloudflare API Token 之前在聊天记录里以明文出现过，属于「已泄露」状态。

| 平台 | 操作 |
|---|---|
| Resend | API Keys → 旧的 **Revoke** → 新建（权限 **Sending access**） |
| Cloudflare | My Profile → API Tokens → 旧的 **Roll / Delete** |
| GitHub | Settings → SSH and GPG keys → 删掉本地那把 `id_ed25519` |

做完之后，再走下面的流程 —— **把轮换后的新 Key 交给对方，别交旧的那把。**

---

### 3.1 代码：GitHub 仓库过户

**方式一：Transfer ownership（保留提交历史、issues）**

```
你的仓库 → Settings → General → 拉到最底 Danger zone → Transfer ownership
填入对方 GitHub 用户名 → 对方邮箱会收到确认邮件，24 小时内接受
```

注意：
- 私有仓库转移要求对方账号有私有仓库额度（免费账号现在有，一般没问题）
- 转移后原 URL 会自动 301 跳转到新地址，但你**不再拥有任何权限**，需要对方重新加你为 collaborator 你才能继续看
- ⚠️ **转移后，Cloudflare 那边的 GitHub 授权会失效**（授权绑的是你的 GitHub 账号），必须在对方 Cloudflare 重新 Connect to Git

**方式二：对方建空仓库，你 push 过去（更简单）**

```bash
# 对方建好空仓库后，你执行
git remote add handover git@github.com:对方用户名/fengyan-website.git
git push -u handover main
```

- 优点：不用等对方接受邀请，不受额度限制
- 缺点：是一次性快照，后续要同步得手动加 remote

> 无论哪种方式，**推完之后立刻删除你本地和 GitHub 上的 SSH Key / Deploy Key**。

---

### 3.2 Cloudflare：对方账号重建 Pages

对方在自己的 Cloudflare 账号里：

```
Workers & Pages → Create → Pages → Connect to Git → 选中刚过户的仓库
```

构建配置（**和现在完全一致，照抄**：

| 配置项 | 值 |
|---|---|
| Framework preset | `None` |
| Build command | **留空** |
| Build output directory | `/` |

然后 Settings → Environment variables 填三个（**Preview 和 Production 都要填**）：

| 变量 | 值 |
|---|---|
| `RESEND_API_KEY` | 对方 Resend 账号生成的新 Key |
| `MAIL_FROM` | `notify@fengyanpigment.com`（等域名在他账号验证通过后再改，先见 3.4） |
| `NOTIFY_EMAIL` | 换成对方的收件邮箱 |

- 项目名可以还叫 `fengyan-website`，跨账号允许同名，pages.dev 子域名也能保持一致
- ⚠️ **改完环境变量必须触发一次重新部署才生效**，这是最容易漏的一步

---

### 3.3 GitHub 授权重新连接

过户/新建仓库后，对方的 Cloudflare 需要重新授权 GitHub：

```
Cloudflare → Settings → Builds & deployments → Manage repositories → 授权对方 GitHub 账号
```

---

### 3.4 Resend：唯一的硬中断点 ⚠️

**同一域名不能在两个 Resend 账号同时验证。** 只要 `fengyanpigment.com` 还在你的账号里处于 Verified 状态，对方就加不进去（会提示域名已被占用）。

所以切换必须这样排：

**第一步：对方先用一个不冲突的发件地址把链路跑通**

未验证域名时，Resend 允许用 `onboarding@resend.dev` 发到账号注册邮箱做测试：

```
对方 Resend → 新建 API Key（Sending access）
MAIL_FROM 临时填: onboarding@resend.dev
NOTIFY_EMAIL 填: 对方的注册邮箱
→ 部署 → 提交一封测试询盘 → 确认能收到
```

这一步能验证「Pages Functions → Resend → 收件箱」整条链路是通的，**而完全不影响线上正在跑的站点**。

**第二步（选低峰期操作）：域名搬家**

```
1. 你：Resend → Domains → fengyanpigment.com → Remove        （发信开始中断）
2. 对方：Resend → Domains → Add Domain → fengyanpigment.com
3. 对方：记下新生成的 SPF / DKIM / DMARC 三条记录值
4. 腾讯云 DNS：把旧的 resend._domainkey 等记录值改成新的
5. 等 Resend 后台状态变绿 Verified                            （发信恢复）
6. 对方：MAIL_FROM 改回 notify@fengyanpigment.com → 重新部署
```

中断窗口 ≈ DNS 生效时间。**提前把 DNS 的 TTL 改成 300 秒**（提前一天改，让旧 TTL 先过期），中断就能控制在 5–10 分钟。

**中断期间会不会丢询盘？不会。** 代码里邮件失败只是返回 `mail.status: skipped`，前端还有 localStorage 兜底，访客照常提交成功，事后能补捞。你要是在意，就挑晚上操作。

---

### 3.5 切 DNS（正式上线）

在腾讯云域名控制台：

| 主机记录 | 类型 | 原记录值 | 新记录值 |
|---|---|---|---|
| `www` | CNAME | `fengyan-website.pages.dev` | 对方的 `<新项目>.pages.dev` |

**提前一天**把这条记录的 TTL 调到 300，切换时才够快。

如果域名也要过户给对方，就走腾讯云「域名过户」流程（需要对方的实名信息模板），过户完再由对方改解析。

---

### 3.6 验收清单

切换完成后，逐条打勾：

- [ ] `https://www.fengyanpigment.com/` 打开正常，样式字体无异常
- [ ] 产品页图片全部加载，无 404
- [ ] `curl -X OPTIONS .../api/submit` 返回 **204**
- [ ] 提交一封真实测试询盘，返回 `ok:true` 且 `mail.status: sent`
- [ ] 对方邮箱**确实收到**邮件，且不在垃圾箱
- [ ] 用手机 4G 打开首页，速度可接受
- [ ] 非白名单来源调用接口返回 **403**（安全加固仍在生效）
- [ ] 对方能自己 push 一次代码并看到自动部署成功

---

### 3.7 收尾：把你这边的资源清干净

验收通过后（建议观察 48 小时再清）：

| 平台 | 动作 |
|---|---|
| Cloudflare | 删除你的 Pages 项目、删除 API Token |
| Resend | Revoke API Key、删除域名验证 |
| GitHub | 退出对方仓库的 collaborator、删除 SSH Key / Deploy Key |
| 本地 | `git remote remove origin`，删掉 SSH 私钥 |
| 域名 | 若不过户，把续费提醒转给对方 |

---

## 四、交付包清单（路径 C 用这个）

打成一个文件夹交给对方：

```
fengyan-website-交付包/
├─ 代码仓库地址 或 fengyan-website.zip（含 .git）
├─ 部署全流程与运维手册.md        ← 已有
├─ 本文件（账号与系统交付手册.md）
├─ 密钥交接表.md                  ← 单独加密，别和代码放一起
├─ DNS 记录快照.csv / 截图        ← 腾讯云控制台导出，含 SPF/DKIM/DMARC/MX
├─ 素材原图/                      ← 如果有需要二次设计的图
└─ 交接验收单.md                  ← 就是 3.6 那张表，双方签字确认
```

**密钥交接表**（示例，实际值单独传）：

| 项目 | 值 | 在哪配置 |
|---|---|---|
| `RESEND_API_KEY` | `re_xxxxxxxx` | Cloudflare Pages 环境变量 |
| `MAIL_FROM` | `notify@fengyanpigment.com` | 同上 |
| `NOTIFY_EMAIL` | `xxx@公司域名.com` | 同上 |
| GitHub 仓库 | `https://github.com/xxx/fengyan-website` | — |
| Cloudflare 账号 | 邮箱 + 密码 | — |
| 域名注册商 | 腾讯云，账号/到期日 | — |

⚠️ **密钥不要走微信、邮件、群聊明文传。** 用密码管理器共享（1Password / Bitwarden），或加密压缩包 + 另一个渠道传密码。

---

## 五、注意事项（这些比操作本身更重要）

### 1. 备案 —— 已决定「暂不备案」

**2026-09-12 决定：不做 ICP 备案**，页脚原有的备案号占位已删除。

背景：域名 `fengyanpigment.com` 尚未备案，整站跑在 Cloudflare Pages 海外节点。未备案的后果是国内访问速度一般、且不能走国内 CDN，但**不影响正常访问**（这点和某些国内平台的硬性拦截不同）。

如果将来要备案：备案要求站点接入境内服务器，届时需要**同时**做两件事——
1. 在页脚补回真实备案号（现在是空的）；
2. 把站点从 Cloudflare 迁回境内平台（腾讯云 EdgeOne / 静态网站托管），否则属于「接入商与实际不符」，有被抽查注销的风险。

**交付给对方时请把这条讲清楚**，别让对方以为页脚只是忘了放。

### 2. 账号主体问题 —— 给企业做就别用个人号

四个账号（GitHub / Cloudflare / Resend / 腾讯云）如果长期挂在个人名下：

- 你离开公司 = 公司失去控制权
- 你账号出问题 = 公司站点瘫痪
- 账单和合规责任算你头上

**正确做法**：用公司邮箱注册一套账号，域名实名认证主体填公司。如果现在还是个人号，趁着这次交付一并迁过去。

### 3. 费用归属

| 服务 | 现在 | 过户后 |
|---|---|---|
| Cloudflare Pages | 免费额度内 | 归对方 |
| GitHub 私有仓库 | 免费 | 归对方 |
| Resend | 3000 封/月免费，**超出会扣你绑的卡** | 归对方，但记得解绑你的支付方式 |
| 域名 | 腾讯云年费 | 要过户就一起过，不然续费提醒留给你 |

**Resend 那条特别注意**：只要域名还在你账号里，超出免费额度的账单就找你。

### 4. 保留回滚能力

旧的那套 Pages 项目**先别删**。DNS 切换后保留 7 天，万一新环境出问题，把 CNAME 改回来 5 分钟就能回滚。

### 5. 别忘了解除自动化连接

如果你本地还有 Git remote、SSH Key、或者我这边的连接器配置指向这些账号，交接完一并清掉，避免哪天误操作推到对方的仓库。

---

## 六、时间预算

| 路径 | 操作时间 | 停机时间 |
|---|---|---|
| B · 只交权限 | ~10 分钟 | 0 |
| C · 交代码对方自建 | 对方 30–60 分钟 | 0（新旧并行） |
| A · 完全过户 | 1–2 小时 | 5–10 分钟（Resend 域名搬家那段） |

DNS 全球生效最长 48 小时，但因为有旧环境兜底，用户侧感知不到。

---

## 七、一句话建议

如果是给公司/客户做的 → 走**路径 C**，并且顺手把四个账号的主体从个人换成公司，域名实名也改到公司名下。
如果只是帮朋友搭的 → 走**路径 B**，先给权限，等他熟悉了再决定要不要过户。
无论走哪条 → **先把 Resend Key 和 Cloudflare Token 轮换一遍**，这两个已经在聊天记录里明文出现过了。
