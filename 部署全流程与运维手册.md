# 峰妍新材料官网 · 部署全流程与运维手册

> 整理时间：2026-08-31
> 适用范围：从「本地 HTML 文件」到「GitHub + Cloudflare Pages + Resend 邮件通知 + 自定义域名」的完整链路

---

## 一、现在跑在哪

| 项目 | 地址 | 说明 |
|---|---|---|
| 正式官网 | https://www.fengyanpigment.com/ | 对外唯一地址 |
| 备用预览 | https://fengyan-website.pages.dev/ | Cloudflare 自带域名，正式域名出问题时可临时顶上 |
| 代码仓库 | https://github.com/wuxueqin1225-source/fengyan-website | 私有仓库，`main` 分支 |
| 邮件服务 | Resend（resend.com） | 免费额度 3000 封/月 |
| 收件邮箱 | 2892910639@qq.com | 询盘直达 |
| 发件地址 | notify@fengyanpigment.com | 域名已通过 Resend 验证 |

---

## 二、整体架构

```
访客浏览器
    │
    ├─ 静态页面/图片/字体 ──→ Cloudflare Pages CDN（全球边缘节点直出）
    │
    └─ 表单提交 /api/submit ─→ Pages Functions（Workers 运行时）
                                      │
                                      └─ fetch → Resend HTTP API → 收件邮箱

DNS：腾讯云域名解析（www → CNAME → fengyan-website.pages.dev）
更新：本地改代码 → git push → GitHub → Cloudflare 自动构建部署（约 1-2 分钟）
```

**关键特征**：全站无数据库、无服务器运维、无构建步骤。43 个产品数据存在 `products.json` 里，随静态资源一起分发。

---

## 三、完整部署流程（从零复现）

### 阶段一：代码改造（适配 Cloudflare）

Cloudflare Pages 的后端叫 **Pages Functions**，代码放在项目根目录的 `functions/` 下，目录结构即路由：

```
functions/api/submit.js  →  处理 POST /api/submit
```

函数写法是标准的 Workers 格式：

```javascript
export async function onRequest(context) {
  const { request, env } = context;   // env 里读环境变量
  // ...
}
```

配套还需要一个 `_routes.json`，告诉 Cloudflare 只有 `/api/*` 走 Functions，其余静态资源直接走 CDN（省调用额度、也更快）：

```json
{
  "version": 1,
  "include": ["/api/*"],
  "exclude": []
}
```

### 阶段二：GitHub 仓库

```bash
cd fengyan-website
git init
git add -A
git commit -m "初始化"
git branch -M main
git remote add origin git@github.com:<用户名>/fengyan-website.git
git push -u origin main
```

需要先生成 SSH 密钥并把公钥加到 GitHub：

```bash
ssh-keygen -t ed25519 -C "fengyan-website" -f ~/.ssh/id_ed25519 -N ""
cat ~/.ssh/id_ed25519.pub      # 复制到 GitHub → Settings → SSH and GPG keys
ssh -T git@github.com          # 验证，看到 "Hi xxx!" 即成功
```

### 阶段三：Cloudflare Pages

控制台路径：Workers & Pages → Create → Pages → Connect to Git

| 配置项 | 值 |
|---|---|
| Framework preset | `None` |
| Build command | **留空** |
| Build output directory | `/` |

首次连接私有仓库需要一次 GitHub 授权，之后每次 push 自动部署。

### 阶段四：Resend 邮件

1. 注册 resend.com
2. Domains → Add Domain → 填 `fengyanpigment.com`
3. 按提示到腾讯云 DNS 添加三条记录（SPF / DKIM / DMARC），等待状态变绿 Verified
4. API Keys → Create，权限选 **Sending access**

需要的三个环境变量：

| 变量 | 值 |
|---|---|
| `RESEND_API_KEY` | `re_` 开头的一串 |
| `MAIL_FROM` | `notify@fengyanpigment.com` |
| `NOTIFY_EMAIL` | `2892910639@qq.com`（多个用英文逗号分隔） |

### 阶段五：自定义域名

1. Cloudflare Pages → Custom domains → 添加 `www.fengyanpigment.com`
2. 腾讯云域名控制台添加解析：

| 主机记录 | 类型 | 记录值 |
|---|---|---|
| `www` | **CNAME** | `fengyan-website.pages.dev` |

3. 等状态从 `pending` 变 `active`，SSL 证书由 Cloudflare 自动签发（Google CA）

---

## 四、关键决策及原因

### 为什么邮件不用 SMTP（nodemailer）

Cloudflare Pages Functions 跑在 **Workers 运行时（V8 isolate）**，没有 Node 的 `net` / `tls` / `dns` 模块。`nodemailer` 依赖这些模块建立 SMTP 连接，开了 `nodejs_compat` 也补不上——它在这里根本跑不起来。

改用 Resend 的 HTTP API 后，代码只剩一个 `fetch` 调用，Workers 原生支持，也没有任何第三方依赖。

### 为什么域名用 CNAME 而不是 NS 托管

| | CNAME（采用） | NS 全托管 |
|---|---|---|
| 腾讯云 DNS | 不动 | 要改 DNS 服务器 |
| 企业邮箱 MX | 完全不受影响 | 需迁移所有解析记录 |
| 其他子域 | 不受影响 | 需全部迁移 |
| SSL | 自动签发 | 自动签发 |

只加一条 CNAME，风险最小。根域名 `fengyanpigment.com` 留给邮箱业务。

### 为什么不用数据库

询盘量一天几封，数据库是过度设计。现在邮件即存档，配合前端 localStorage 兜底，即使邮件服务故障也不会丢询盘。

如果以后需要备份，在 Pages 绑定一个 KV 命名空间（变量名 `INQUIRY_KV`），代码会自动多写一份，无需改代码。

---

## 五、踩坑记录（最重要的一节）

### 1. `204` 响应不能带 body —— 会让表单彻底失效

```javascript
// ❌ 错误：空字符串也算 body，会抛 TypeError
return new Response('', { status: 204, headers: CORS });

// ✅ 正确
return new Response(null, { status: 204, headers: CORS });
```

WHATWG 规范规定 204 响应不允许携带 body。传 `''` 会导致 Function 抛异常返回 500 → 浏览器 CORS 预检失败 → 跨域 POST 被拦截 → **表单一点反应都没有，一个询盘都发不出去**。

这类问题光看代码发现不了，必须实际跑一次才能暴露。

### 2. GitHub MCP 的 `push_files` 会损坏二进制文件

`push_files` 把文件内容当 UTF-8 文本处理，推送 jpg / woff2 会被写坏，表现为"字体变了、图片裂了"，很难排查。**静态资源一律走 SSH `git push`。**

另外 MCP 连接器没有创建仓库的权限（`403 Resource not accessible by integration`），仓库必须手动建。

### 3. `git commit --amend` 默认不改作者

```bash
# ❌ 只改 committer，author 不变
git commit --amend --no-edit

# ✅ 加 --reset-author 才改得掉 author
git commit --amend --reset-author --no-edit
git push -f origin main
```

### 4. Cloudflare API 写环境变量的正确姿势

| 端点 | 结果 |
|---|---|
| `PUT .../deployment_configs` | ❌ 405 method_not_allowed |
| `PUT .../secrets` | ❌ 405 |
| **`PATCH .../pages/projects/{项目名}`** | ✅ 可用 |

```bash
curl -X PATCH "https://api.cloudflare.com/client/v4/accounts/$ACC/pages/projects/fengyan-website" \
  -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
  -d '{"deployment_configs":{"production":{"env_vars":{"KEY":{"value":"值"}}},"preview":{...}}}'
```

触发重新部署：

```bash
curl -X POST ".../pages/projects/fengyan-website/deployments" \
  -H "Authorization: Bearer $TOKEN" -d '{"branch":"main"}'
```

**改完环境变量必须触发一次新部署才生效**，这是最容易漏的一步。

### 5. Cloudflare API 返回的是带缩进的 JSON

```bash
# ❌ 取不到值（冒号后有空格）
grep -o '"id":"[^"]*"'

# ✅ 正确
grep -o '"id"[[:space:]]*:[[:space:]]*"[^"]*"'
```

### 6. 中文路径导致 git 远端引用异常

项目在 `D:\workbuddy数据存储站\...`，`origin/main` 的 remote-tracking ref 建不出来，`git ls-tree origin/main` 报 `Not a valid object name`，但 `git push` / `git fetch` 本身正常。

**验证远端内容要改用 GitHub API，别依赖 `git ls-tree origin/main`。**

### 7. 自定义域名首次访问可能 SSL 握手失败

证书签发后需要时间铺到全球边缘节点，刚配置完可能遇到 `curl error 35`。等几分钟再测即可，不是配置错误。

### 8. 静态 HTML 里的 `\n` 不会换行

```html
<!-- ❌ 页面会原样显示反斜杠 n -->
<div class="addr-value">座机 021-56690218\n手机 15821682128</div>

<!-- ✅ -->
<div class="addr-value">座机 021-56690218<br>手机 15821682128</div>
```

`white-space: pre-line` 只对**真实换行符**生效，对字面量 `\n`（反斜杠+n）无效。

---

## 六、日常维护

### 改文案 / 改产品 / 改样式

```
告诉助手要改什么 → 助手改 index.html 或 products.json → git push → 1-2 分钟自动上线
```

不需要登录任何控制台。

### 手动部署

Cloudflare Pages → 项目 → Deployments → Retry deployment，或在 GitHub 上触发一次空提交。

### 查看部署日志

Cloudflare Pages → 项目 → Deployments → 点某次部署看构建日志。

### 查看询盘

邮件即询盘。如果之后想导出成表格，绑定 KV 后可用带 token 的导出接口（需要额外开发）。

---

## 七、故障排查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 表单提交无反应 | 环境变量未配，或 204 预检失败 | 先用 curl 测 `OPTIONS /api/submit` 是否返回 204 |
| 邮件收不到 | 垃圾箱 / Resend 域名未验证 / 配额用尽 | 查 Resend 后台 Logs 看投递状态 |
| 页面 404 | 部署失败或文件路径错 | 查 Cloudflare Deployments 日志 |
| 域名访问不了 | CNAME 未生效或证书未就绪 | `nslookup www.fengyanpigment.com` 看是否解析到 Cloudflare IP |
| 样式/字体异常 | 二进制资源传输损坏 | 对比文件大小与本地是否一致 |

**快速自检命令**：

```bash
# 预检（应返回 204）
curl -X OPTIONS -o /dev/null -w "%{http_code}\n" https://www.fengyanpigment.com/api/submit

# 提交测试询盘（应返回 ok:true 且 mail.status=sent）
curl -X POST https://www.fengyanpigment.com/api/submit \
  -H "Content-Type: application/json" \
  -d '{"name":"测试","phone":"13800000000","products":"柠檬黄","desc":"联调"}'

# 看首页响应速度
curl -o /dev/null -w "HTTP %{http_code} | 首字节 %{time_starttransfer}s\n" https://www.fengyanpigment.com/
```

---

## 八、安全清单

- [ ] **不要在代码里写任何密钥**。API Key 一律放 Cloudflare 环境变量
- [ ] **不要把 `.env` 提交到 Git**。当前项目没有 `.env`，环境变量全在云端
- [ ] Resend API Key 权限保持 **Sending access**（最小权限），泄露也只能发信
- [ ] Cloudflare API Token 用完后建议删除或轮换，需要时再建
- [ ] 定期用 `git log -p` 检查有没有误提交敏感信息
- [ ] 仓库设为 Private，避免源码和设计稿公开

**如果密钥不慎泄露**：立即到对应平台 Revoke（Resend → API Keys → 删除；Cloudflare → API Tokens → Roll），然后重新生成并更新环境变量 + 重新部署。

---

## 九、待优化项

| 项目 | 说明 | 优先级 |
|---|---|---|
| 未引用素材清理 | `hero-pigment.jpg`、名片、3 张色卡共约 1.34MB 未被引用，可移出仓库减小体积 | 低 |
| 图片压缩 | `about-office.jpg` 1.6MB 偏大，可压缩到 300KB 以内提升首屏 | 中 |
| 根域名跳转 | `fengyanpigment.com` → `www.fengyanpigment.com` 的 301 重定向 | 中 |
| 国内速度实测 | 用手机 4G/5G 流量测首屏，若明显慢再考虑腾讯云备案方案 | 中 |
| 响应式检查 | 多尺寸断点下的布局验证 | 未完成 |
| KV 存档 | 绑定后可做询盘备份与 CSV 导出 | 可选 |

---

## 十、费用

| 服务 | 费用 |
|---|---|
| Cloudflare Pages | 免费（含Functions、SSL、全球 CDN、每月 500 次构建） |
| GitHub 私有仓库 | 免费 |
| Resend | 免费额度 3000 封/月，超出后按量计费 |
| 域名 | 腾讯云年费（已有） |

按当前询盘量，**日常运营成本为零**。
