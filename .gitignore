node_modules/
.wrangler/
.workbuddy/
*.log
.DS_Store
Thumbs.db

# 本地留档：已下线 / 已被替换的历史资产。
# 这些目录不进仓库、也不参与任何平台的部署，只为本地备查与回滚。
# 需要恢复某一份文件时，从 git 历史取（见 README「历史方案」一节）。
_deprecated-cloudbase-cloudfunctions/
_deprecated-edgeone-makers/
_originals/
